extends layout

block content
	form(action='', method='POST', onsubmit='return false; alert("fuck you");')
	each param in params
		.field
			label
				span= param.name
				input(type=param.type, name=param.name, data-restrictions=param.restrictions.join(' '))
	button LOL

